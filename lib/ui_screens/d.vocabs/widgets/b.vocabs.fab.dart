part of '../_index.dart';

class VocabsFab extends StatelessWidget {
  const VocabsFab({super.key});

  @override
  Widget build(BuildContext context) {
    return FloatingActionButton(
      child: const Icon(Icons.add),
      onPressed: () {},
    );
  }
}
